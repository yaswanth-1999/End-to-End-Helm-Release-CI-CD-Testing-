image:
  repository: yaswanth1999/myapp
  tag: v1

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  host: myapp.local

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 50
